{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for the dictation overlay. It only listens for state_changed, so it gets no plugin permissions — not even os.",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}
