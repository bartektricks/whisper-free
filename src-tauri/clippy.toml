# Product, vendor, and framework names that appear in prose rather than as code.
# `..` keeps clippy's own default list of known identifiers.
doc-valid-idents = ["LocalDictation", "CoreAudio", "HuggingFace", ".."]
